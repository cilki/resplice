[package]
name = "resplice-macros"
version = "0.0.3"
description = "Rewrite it in Rust"
edition = "2024"
license = "Unlicense"

[lib]
proc-macro = true

[dependencies]
syn = { version = "3.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"
